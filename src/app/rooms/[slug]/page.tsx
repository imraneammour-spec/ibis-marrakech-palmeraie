import { Metadata } from "next"; import { notFound } from "next/navigation"; import Link from "next/link"; import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { getRoomBySlug, rooms } from "@/data/rooms";
export function generateStaticParams(){return rooms.map(({slug})=>({slug}));} export function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{return params.then(({slug})=>{const room=getRoomBySlug(slug);return {title:room?`${room.name} | ibis Marrakech Palmeraie`:"Room not found"};});}
export default async function RoomPage({params}:{params:Promise<{slug:string}>}){const room=getRoomBySlug((await params).slug);if(!room)notFound();return <><div className="pageHero"><SiteHeader/><div><p className="eyebrow">{room.capacity} · {room.bed}</p><h1>{room.name}</h1></div></div><main className="roomDetail section"><p className="lead">{room.description}</p><div className="detailGrid"><div><p className="eyebrow">The essentials</p><p>Private bathroom<br/>Desk area<br/>Television<br/>Free Wi-Fi</p></div><div><p className="eyebrow">Good to know</p><p>Capacity: {room.capacity}<br/>Bedding: {room.bed}{room.view&&<><br/>View: {room.view}</>}</p></div></div><a className="button" href="#book">Book your stay</a><p><Link className="textLink" href="/rooms">All rooms ←</Link></p></main><SiteFooter/></>}
